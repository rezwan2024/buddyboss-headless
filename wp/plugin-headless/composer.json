{
  "name": "buddyboss-headless/headless-auth",
  "description": "Dev tooling only (PHPUnit) for the Headless Auth WP plugin. Not deployed — see ../../scripts/push-plugin.",
  "private": true,
  "require-dev": {
    "phpunit/phpunit": "^11.0"
  },
  "scripts": {
    "test": "phpunit"
  }
}
